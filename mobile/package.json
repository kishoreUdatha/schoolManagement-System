{
  "name": "brightcampus-parent-app",
  "version": "1.0.0",
  "private": true,
  "description": "The BrightCampus parent app, wrapped for Android.",
  "scripts": {
    "sync": "cap sync android",
    "apk": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
