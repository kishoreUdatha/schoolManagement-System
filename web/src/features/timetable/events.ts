// Page-head buttons talk to the live component through these window events.
// Kept out of the "use client" files so server pages can import them as strings.

export const BLOCK_EVENT = "tt:block-out";
export const ASSIGN_EVENT = "tt:assign-substitute";
