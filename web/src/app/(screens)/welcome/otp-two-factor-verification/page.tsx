// SCR-006 · OTP / Two-Factor Verification
// Module: Public Website & Authentication · Role: All Users · Release: Phase 2 · Stories: US-0011 / US-0012
// Mock: screens/SCR-006_OTP_Two_Factor_Verification.html
// Backend: the old frontend served this at /parent/login — Two-step when the school requires it; no tokens without the code
// Generated by scripts/build_pages.py. Sample data only until this page is wired to the API.

import Link from "next/link";
import { HeroArt } from "@/components/ui/HeroArt";
import { Icon } from "@/components/ui/Icon";

export const metadata = { title: "SCR-006 · OTP / Two-Factor Verification · BrightCampus" };

export default function Page() {
  return (
    <>
      <div className="auth-page">
        <section className="auth-visual">
          <Link href="/screens" className="brand">
            <span className="brand-mark">
              <Icon name="book" />
            </span>
            <span>
              BrightCampus
              <small>SCHOOL ERP</small>
            </span>
          </Link>
          <div className="auth-copy">
            <h1>
              Every school day.
              <br />
              A little brighter.
            </h1>
            <p>
              One welcoming space for learning, teaching and everything that makes a school thrive.
            </p>
          </div>
          <HeroArt />
          <span className="art-note">BrightCampus · Connected school life</span>
        </section>
        <main className="auth-main">
          <div className="auth-top">
            {"Bright International School "}
            <Link href="/welcome/workspace-role-selection">Change workspace</Link>
          </div>
          <form className="auth-form" id="auth-form" data-auth-screen="6">
            <h1>Check your verification code</h1>
            <p>Enter the 6-digit code sent to your registered mobile ending in 1021.</p>
            <div className="otp-fields">
              <input type="text" inputMode="numeric" pattern="[0-9]" maxLength={1} required aria-label="Digit 1" defaultValue="4" />
              <input type="text" inputMode="numeric" pattern="[0-9]" maxLength={1} required aria-label="Digit 2" defaultValue="8" />
              <input type="text" inputMode="numeric" pattern="[0-9]" maxLength={1} required aria-label="Digit 3" defaultValue="2" />
              <input type="text" inputMode="numeric" pattern="[0-9]" maxLength={1} required aria-label="Digit 4" defaultValue="9" />
              <input type="text" inputMode="numeric" pattern="[0-9]" maxLength={1} required aria-label="Digit 5" defaultValue="1" />
              <input type="text" inputMode="numeric" pattern="[0-9]" maxLength={1} required aria-label="Digit 6" defaultValue="6" />
            </div>
            <button type="button" className="btn primary" data-auth-verify="">
              <Icon name="check" className="sm" />
              Verify code
            </button>
            <p className="auth-note">
              {"Didn’t get the code? "}
              <a href="#" data-action="Resend verification code">Resend code</a>
            </p>
            <div className="auth-note">Sample sign-in flow for design review</div>
          </form>
          <div className="auth-help">Need help? Contact your school administrator.</div>
        </main>
      </div>
    </>
  );
}
