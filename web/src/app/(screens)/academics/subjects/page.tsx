// SCR-096 · Subjects
// Module: Academics & Curriculum · Role: Academic Coordinator · Release: MVP · Stories: US-0191 / US-0192
// Mock: screens/SCR-096_Subjects.html
// Backend: the old frontend served this at /school/subjects — CRUD, code, kind, department, bulk paste
// Generated by scripts/build_pages.py. Sample data only until this page is wired to the API.

import { AppShell } from "@/components/shell/AppShell";
import { DataTable } from "@/components/ui/DataTable";
import { Icon } from "@/components/ui/Icon";
import { Panel } from "@/components/ui/primitives";

export const metadata = { title: "SCR-096 · Subjects · BrightCampus" };

// Table columns
const columns = ["Subject", "Subject code", "Department", "Subject type", "Classes", "Status"];

// Sample records — replace with the API response
const rows = [
  ["Mathematics", "MAT", "Mathematics", "Core", "8 A, 8 B", "Active"],
  ["Science", "SCI", "Science", "Core", "7 A, 7 B", "Active"],
  ["English", "ENG", "Languages", "Language", "6 A, 6 B", "Pending"],
  ["Social Studies", "SST", "Social Sciences", "Elective", "9 A, 9 B", "Active"],
  ["Hindi", "HIN", "Administration", "Core", "8 A, 8 B", "Active"],
  ["Computer Science", "CSC", "Languages", "Core", "7 A, 7 B", "On leave"],
];

export default function Page() {
  return (
    <AppShell screen="SCR-096" actions={<>
        <button type="button" className="btn" data-export="">
          <Icon name="download" className="sm" />
          Export
        </button>
        <button type="button" className="btn primary" data-action="Add subject">
          <Icon name="plus" className="sm" />
          Add subject
        </button>
      </>}>
      <div className="filterbar">
        <div className="searchbox">
          <Icon name="search" className="sm" />
          <input data-table-search="" placeholder="Search subjects…" aria-label="Search records" />
        </div>
        <select aria-label="Filter category" data-filter-col="auto">
          <option>All classes</option>
          <option>Class 8</option>
          <option>Class 7</option>
          <option>Class 6</option>
        </select>
        <select aria-label="Filter status" data-filter-col="status">
          <option>All statuses</option>
          <option>Active</option>
          <option>Pending</option>
          <option>Approved</option>
          <option>Paid</option>
          <option>Overdue</option>
          <option>Published</option>
          <option>Verified</option>
          <option>Available</option>
        </select>
        <button type="button" className="btn" data-action="Choose report date">
          <Icon name="calendar" className="sm" />
          21 Sep 2026
        </button>
      </div>
      <Panel title="All records" sub="Current academic year · Main Campus" action={<button type="button" className="btn" data-columns="">
          <Icon name="grid" className="sm" />
          Columns
        </button>} flush>
        <DataTable columns={columns} rows={rows} />
      </Panel>
    </AppShell>
  );
}
