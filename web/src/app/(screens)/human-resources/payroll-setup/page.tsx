// SCR-183 · Payroll Setup
// Module: HR / Leave / Payroll · Role: HR · Release: Phase 3 · Stories: US-0365 / US-0366
// Mock: screens/SCR-183_Payroll_Setup.html
// Backend: the old frontend served this at /school/payroll — Salary structure plus statutory rates
// Generated by scripts/build_pages.py. Sample data only until this page is wired to the API.

import { AppShell } from "@/components/shell/AppShell";
import { DataTable } from "@/components/ui/DataTable";
import { Icon } from "@/components/ui/Icon";
import { Panel } from "@/components/ui/primitives";

export const metadata = { title: "SCR-183 · Payroll Setup · BrightCampus" };

// Table columns
const columns = ["Component", "Amount", "Frequency", "Taxable", "Status"];

// Sample records — replace with the API response
const rows = [
  ["Basic pay", "₹36,000", "Monthly", "Yes", "Active"],
  ["House rent allowance", "₹6,000", "Monthly", "Yes", "Active"],
  ["Other allowance", "₹3,000", "Monthly", "Yes", "Active"],
];

export default function Page() {
  return (
    <AppShell screen="SCR-183" actions={<button type="button" className="btn primary" data-submit-main="">
        <Icon name="check" className="sm" />
        Save payroll setup
      </button>}>
      <div className="stack">
        <form id="main-form" className="panel">
          <div className="panel-head">
            <h2>Salary structure</h2>
          </div>
          <div className="panel-body">
            <div className="form-grid">
              <label className="field">
                <span>
                  Salary structure
                  <span className="req">*</span>
                </span>
                <input type="text" placeholder="Enter salary structure" name="Salary structure" aria-label="Salary structure" required defaultValue="Teaching staff · Standard" />
              </label>
              <label className="field">
                <span>
                  Pay cycle
                  <span className="req">*</span>
                </span>
                <input type="text" placeholder="Enter pay cycle" name="Pay cycle" aria-label="Pay cycle" required defaultValue="Monthly" />
              </label>
              <label className="field">
                <span>
                  Basic pay
                  <span className="req">*</span>
                </span>
                <input type="text" placeholder="Enter basic pay" name="Basic pay" aria-label="Basic pay" required defaultValue="₹36,000" />
              </label>
            </div>
          </div>
        </form>
        <Panel title="Salary components" sub="Add or edit the applicable components" action={<button type="button" className="btn" data-action="Add component">
            <Icon name="plus" className="sm" />
            Add component
          </button>} flush>
          <DataTable columns={columns} rows={rows} selectable={false} />
        </Panel>
      </div>
    </AppShell>
  );
}
