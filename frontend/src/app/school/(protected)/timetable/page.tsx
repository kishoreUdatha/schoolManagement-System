"use client";

import { TimetableWorkspace } from "@/components/timetable/TimetableWorkspace";

export default function TimetablePage() {
  return <TimetableWorkspace periodsHref="/school/periods" />;
}
