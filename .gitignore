# Env files
.env
.env.local
.env.*.local
*.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# Node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE
.vscode/
.idea/
.claude/settings.local.json
.claude/*.lock
*.swp
*.swo
.DS_Store
Thumbs.db

# Docker
*.pid

# Logs
logs/
*.log

# Local data
postgres-data/
redis-data/
uploads/

# OS
desktop.ini

# Uploaded files (local storage backend)
backend/storage/

# Design handoffs. 223MB between them, mostly PNG and the same screens
# again as a zip and as the folder it was unzipped into. The mocks are the
# reference for the UI, not an input to the build.
Mock Screen/
Parent_Mobile_58_Screens*

# Backups of the two trackers that are tracked, written beside them by the
# scripts that edit them. The originals stay in git; these are the undo.
*.backup.xlsx

# An empty file someone saved a URL into. Not ignored because it is big —
# ignored because there is nothing in it.
httpswww.myclassboard reference.txt

# Agent worktrees
.claude/worktrees/
